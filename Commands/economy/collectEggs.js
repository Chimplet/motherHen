const { MessageEmbed } = require('discord.js');

module.exports = {
    name: 'collecteggs',
    description: 'Collect eggs passively laid by your hens.',
    execute(client, interaction, arguments) {

    }
}